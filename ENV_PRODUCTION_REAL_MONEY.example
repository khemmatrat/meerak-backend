# =============================================================================
# ENVIRONMENT CONFIG — PRODUCTION REAL MONEY
# =============================================================================
# Copy to .env and set values. Never commit .env to version control.

# --- Backend (Node) ---
NODE_ENV=production

# JWT: Required for production. All wallet/recon/admin/audit APIs require Bearer token.
# Token payload must include: sub (user id), optional role (USER|ADMIN|AUDITOR).
JWT_SECRET=your-256-bit-secret-here

# PostgreSQL (ledger, wallets, reconciliation, audit)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=meerak
DB_USER=meerak_app
DB_PASSWORD=secure-password

# Redis (optional; webhook idempotency)
REDIS_URL=redis://localhost:6379

# CORS (frontend origin)
CORS_ORIGIN=https://your-app.example.com

# --- Frontend (Vite build) ---
# Set in build env or .env.production:
# VITE_REAL_MONEY_MODE=true
# VITE_API_URL=https://api.your-app.example.com

# When VITE_REAL_MONEY_MODE=true:
# - All wallet topup/withdraw MUST use backend.
# - No Firebase fallback; backend downtime fails transaction.
# - Firebase balance sync is disabled on success (return profile with backend balance).
