-- =================================================================================
-- 175: ปล่อย/ระงับการรับโค้ดแยกจากการแสดงแบนเนอร์ (โฆษณาล่วงหน้าได้)
-- =================================================================================
-- promo_claims_enabled = FALSE → แสดงแบนเนอร์ได้ (ตาม is_active + ช่วงวันแสดง) แต่ห้ามรับ/ใช้โค้ด
-- =================================================================================

ALTER TABLE home_banners
  ADD COLUMN IF NOT EXISTS promo_claims_enabled BOOLEAN NOT NULL DEFAULT TRUE;

COMMENT ON COLUMN home_banners.promo_claims_enabled IS 'FALSE = ระงับรับ/ใช้โค้ดชั่วคราว — แบนเนอร์ยังโชว์เป็นโฆษณาได้';
