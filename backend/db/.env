# ไม่ใช้ไฟล์นี้ — ใช้ root .env เท่านั้น
# การตั้งค่า DB (DB_HOST, DB_PORT, DB_DATABASE, DB_USER, DB_PASSWORD, DATABASE_URL)
# อยู่ที่โฟลเดอร์โปรเจกต์: g:\meerak\.env
# Backend และทุกสคริปต์ migration โหลดจาก root .env แล้ว
