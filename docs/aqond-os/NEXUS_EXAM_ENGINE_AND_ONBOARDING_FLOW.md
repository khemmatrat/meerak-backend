# Nexus Exam Engine & Onboarding Flow

## 0. สรุปการตรวจสอบ (ลำดับขั้นตอน / การทำงาน / เป้าหมาย)

- **ลำดับขั้นตอน:** Registration → Identity (KYC) → Professional Test (NexusExamEngine) → Matching → Job Start → Finished
- **การทำงาน:** สมัคร → อัปโหลดบัตร/รูปคู่บัตร → เรียน Academy + สอบ M1 (55 ข้อ 85%) → M2 (36 ข้อ/อาชีพ 80%) → M3 (Scenario เรียนรู้) → Admin อนุมัติ → รับงานได้ → Escrow + ประกัน → จบงานด้วย GPS/OTP/Audit
- **เป้าหมาย:** คัดกรองมิจฉาชีพ (KYC), คัดกรองผู้ขาดความรับผิดชอบ (Exam), ประกันความเสี่ยงทางการเงิน (Escrow/ประกัน), ยืนยันความโปร่งใส (Audit)

---

## 1. ลำดับขั้นตอนระบบ (System Flow) และเป้าหมาย

**การทำงานของระบบ (เรียงตามลำดับ):**

| ขั้น | ชื่อ | เป้าหมาย | สถานะ |
|------|------|----------|--------|
| 1 | **Registration** | สมัครสมาชิก เก็บข้อมูลเบื้องต้น | ✅ มีแล้ว |
| 2 | **Identity (KYC)** | อัปโหลดบัตร + รูปคู่บัตร คัดกรองมิจฉาชีพ/อาชญากร | ✅ มีแล้ว |
| 3 | **Professional Test** | ทำแบบทดสอบวัดจิตสำนึก (NexusExamEngine M1→M2→M3) คัดกรองผู้ขาดความรับผิดชอบ | 🔶 M1 มีแล้ว, M2/M3 กำลังทำ |
| 4 | **Matching** | เปิดระบบให้เห็นงาน เริ่มต้นการทำงานระดับมืออาชีพ | ✅ มีแล้ว |
| 5 | **Job Start** | เก็บเงินเข้า Escrow + ประกันภัย ประกันความเสี่ยงทางการเงิน | ✅ มีแล้ว |
| 6 | **Finished** | ตรวจ GPS + OTP + Audit Log ยืนยันความโปร่งใส | ✅ มีแล้ว |

**เป้าหมายรวม:** คัดกรองผู้ให้บริการ (Provider) ตั้งแต่ขั้นสมัคร → ระบุตัวตน (KYC) → ทดสอบจิตสำนึกและทักษะ (Exam) → รับงานได้หลัง Admin อนุมัติ → งานจบด้วยการยืนยันความโปร่งใส

---

## 2. โครงสร้างข้อสอบ NexusExamEngine (3 Modules)

### Module 1 — General Ethics, Safety, and Law
- **จำนวนข้อ:** 55
- **เกณฑ์ผ่าน:** ≥ 85%
- **ไม่ผ่าน:** ล็อก 24 ชั่วโมง ก่อนทำใหม่
- **สถานะ:** ✅ **มีแล้ว** — คอร์ส `nexus-professional-standards`, Backend `POST /api/provider-onboarding/submit-exam`

### Module 2 — Technical Skills (ตามอาชีพ)
- **จำนวนข้อ:** 36 ข้อต่อ 1 หมวดอาชีพ (รองรับ 20 อาชีพ)
- **เกณฑ์ผ่าน:** ≥ 80%
- **ไม่ผ่าน:** ทำใหม่ได้
- **สถานะ:** ✅ **Backend มีแล้ว** — `GET /api/nexus-exam/questions?module=2&category=...`, `POST /api/nexus-exam/submit` (สลับข้อ/ตัวเลือก); Frontend ยังต้องมีหน้า Quiz + เลือกอาชีพ

### Module 3 — Scenario: Problem Solving & Positive Mindset
- **รูปแบบ:** แบบ Scenario Based
- **หลังตอบแต่ละข้อ:** แสดง "Recommended Action" เพื่อให้เรียนรู้แนวทางปฏิบัติ
- **วัตถุประสงค์:** เรียนรู้ ไม่ใช่ตัดคะแนน (ผ่านอัตโนมัติ)
- **สถานะ:** ✅ **Backend มีแล้ว** — GET questions (มี recommended_action), POST submit บันทึกผล; Frontend ยังต้องมีหน้า Scenario + แสดง Recommended Action

---

## 3. Features ที่ต้องมี (NexusExamEngine)

| Feature | Module 1 | Module 2 | Module 3 | สถานะ |
|---------|----------|----------|----------|--------|
| Randomization (สลับข้อ + ตัวเลือก) | ต้องมี | ต้องมี | ต้องมี | 🔲 Backend/Frontend |
| Time limit ต่อ module | ต้องมี | ต้องมี | ต้องมี | 🔲 |
| บันทึกคะแนนลง `user_exam_results` | ต้องมี | ต้องมี | ต้องมี | ✅ M1/M2/M3 บันทึกแล้ว |
| อัปเดต `onboarding_status` เป็น `QUALIFIED` | เมื่อผ่าน **Module 1 + Module 2** เท่านั้น | - | - | ✅ ผ่าน M1+M2 → QUALIFIED; VERIFIED เฉพาะ Admin |
| Randomization | ต้องมี | ต้องมี | ต้องมี | ✅ Backend สลับข้อและตัวเลือก |
| UI แบบ Luxury Minimalist | ต้องมี | ต้องมี | ต้องมี | ปรับได้ |
| Certificate of Readiness | แสดงเมื่อทำครบ Module 1+2+3 | - | - | 🔲 Frontend |

---

## 4. ลำดับขั้นตอน Onboarding (หลังสมัคร + ผ่าน KYC)

1. **Academy Page** — เรียนบทเรียนสั้นๆ (VDO/Slide)
2. **Start Exam**
   - ทำ **ส่วนที่ 1** (55 ข้อ) → ผ่านแล้วไปต่อ
   - ทำ **ส่วนที่ 2** (36 ข้อ ตามอาชีพ) → ผ่านแล้วไปต่อ
   - ทำ **ส่วนที่ 3** (ทัศนคติ / Scenario) → ทำเพื่อเรียนรู้แนวทางปฏิบัติ
3. **Approval** — แอดมินตรวจผลสอบและกดอนุมัติ (Final Manual Check) → ตั้ง `provider_status = VERIFIED_PROVIDER`

---

## 5. สิ่งที่มีอยู่แล้ว (นำมาใช้ได้)

- **Module 1:** คอร์ส `nexus-professional-standards` (55 ข้อ, 85%, 24h lock), Backend submit-exam, บันทึกลง `user_exam_results`, ผ่านแล้วตั้ง `onboarding_status = MODULE1_PASSED` (ยังไม่ VERIFIED)
- **Module 2:** Backend `GET /api/nexus-exam/questions?module=2&category=...`, `POST /api/nexus-exam/submit` (36 ข้อ, 80%), ผ่าน M1+M2 → `onboarding_status = QUALIFIED`
- **Module 3:** Backend GET questions (scenario + recommended_action), POST submit บันทึกผล (เรียนรู้)
- **Provider status:** `UNVERIFIED` → `PENDING_TEST` (หลัง KYC) → ผ่าน M1 = MODULE1_PASSED, ผ่าน M1+M2 = QUALIFIED → **VERIFIED_PROVIDER เฉพาะเมื่อ Admin กด Approve**
- **Training UI:** TrainingDashboard, TrainingCourse, TrainingQuizPage, Quiz component
- **Certificate:** CertificateView, certificateService (สำหรับคอร์สทั่วไป)
- **Admin:** ปุ่ม "อนุญาตให้เป็นผู้รับงาน" (Approve as Provider) = ตั้ง `provider_status = VERIFIED_PROVIDER`

---

## 6. สิ่งที่ต้องทำเพิ่ม

1. **Backend (ทำแล้ว)** ✅
   - ตาราง `user_exam_results`, คอลัมน์ `onboarding_status`
   - M1 บันทึกผล + ไม่ตั้ง VERIFIED; ผ่าน M1+M2 → QUALIFIED
   - API Module 2/3: GET questions (สลับข้อ/ตัวเลือก), POST submit, บันทึกผล
   - (Time limit ต่อ module — เก็บเวลาเริ่ม/จบ ตรวจที่ Frontend หรือเพิ่มใน Backend ภายหลัง)

2. **Frontend**
   - โครง Module 2: เลือกอาชีพ → แสดง Quiz 36 ข้อ, 80% ผ่าน
   - โครง Module 3: แบบ Scenario + แสดง "Recommended Action" หลังแต่ละข้อ
   - หน้ารวมผลสอบ / Certificate of Readiness (Luxury Minimalist) เมื่อครบ M1+M2 (+ M3)
   - Academy page ชัดเจน: ลำดับ VDO/Slide → สอบส่วน 1 → 2 → 3

3. **Admin**
   - หน้า/คอลัมน์แสดงผลสอบแต่ละ module (จาก user_exam_results)
   - ปุ่ม Approve (Final Manual Check) มีอยู่แล้ว — อาจเพิ่มให้เห็นสถานะ "ผ่าน M1+M2 รออนุมัติ"

---

## 7. การแมปสถานะ

| สถานะในระบบ | ความหมาย |
|-------------|----------|
| `UNVERIFIED` | ยังไม่เริ่ม / ยังไม่ผ่าน KYC |
| `PENDING_TEST` | ผ่าน KYC แล้ว ต้องทำแบบทดสอบ (Module 1 เป็นอย่างน้อย) |
| ผ่าน M1, ยังไม่ผ่าน M2 | เก็บใน user_exam_results; ยังไม่ QUALIFIED |
| ผ่าน M1 + M2 | สามารถตั้งเป็น `QUALIFIED` หรือให้ Admin อนุมัติเป็น `VERIFIED_PROVIDER` |
| `VERIFIED_PROVIDER` | รับงานได้ (หลัง Admin กด Approve หรือระบบอัตโนมัติถ้าตั้งไว้) |
