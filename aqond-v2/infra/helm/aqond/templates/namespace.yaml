apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.global.namespace }}
  labels:
    {{- include "aqond.labels" . | nindent 4 }}
    pod-security.kubernetes.io/enforce: {{ .Values.security.podSecurity.enforce }}
